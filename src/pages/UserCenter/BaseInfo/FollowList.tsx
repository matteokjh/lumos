import React from 'react'
import { UserProps } from '@/types/user'

interface FollowProps {
    dataList: UserProps[]
}
const FollowList = (props: FollowProps) => {
    // const { dataList } = props

    return <div className="FollowList">
        FollowList
    </div>
}

export default FollowList