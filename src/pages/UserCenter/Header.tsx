import React from 'react'
import { Layout } from 'antd'

const Header = () => {
    return (
        <Layout>
            <div className="avatar" style={{
                // backgroundImage: url()
            }}></div>
        </Layout>
    )
}

export default Header